{
  "type": "object",
  "$id": "schema:user:getUser",
  "anyOf": [
    { "required": ["username"] },
    { "required": ["email"] }
  ],
  "properties": {
    "username": { "type": "string" },
    "email": { "type": "string", "format": "email" }
  }
}