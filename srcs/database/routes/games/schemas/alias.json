{
  "type": "object",
  "$id": "schema:games:tournamentAlias",
  "required": [
    "alias"
  ],
  "properties": {
    "newNickname": {
      "type": "string",
      "pattern": "^[a-zA-Z0-9_]{3,20}$",
      "minLength": 3,
      "maxLength": 20
    }
  }
}