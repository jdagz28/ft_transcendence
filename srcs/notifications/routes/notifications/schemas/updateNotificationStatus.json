{
  "type": "object",
  "$id": "schema:notifications:updateNotificationStatus",
  "required": [ "status" ],
  "properties": {
    "status": {
      "type": "string",
      "enum": [ "read", "unread" ]
    }
  }
}