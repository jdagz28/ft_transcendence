{
  "type": "object",
  "$id": "schema:users:changeUsername",
  "required": [
    "changeUsername"
  ],
  "properties": {
    "changeUsername": {
      "type": "string",
      "pattern": "^[a-zA-Z0-9_]{3,20}$",
      "minLength": 3,
      "maxLength": 20
    }
  }
}