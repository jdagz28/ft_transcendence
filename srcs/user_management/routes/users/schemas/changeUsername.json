{
  "type": "object",
  "$id": "schema:users:changeUsername",
  "required": [
    "newUsername"
  ],
  "properties": {
    "newUsername": {
      "type": "string",
      "pattern": "^[a-zA-Z0-9_]{3,20}$",
      "minLength": 3,
      "maxLength": 20
    }
  }
}